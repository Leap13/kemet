<?php
/**
 * Style Variations Support
 *
 * @package Kemet
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * WordPress automatically detects style variations from the /styles/ folder
 * No need to explicitly add 'style-variations' support - it's automatic!
 * Theme support is now handled in class-kemet-after-setup-theme.php
 */

/**
 * Style variations are now properly configured with JSON files in the /styles/ directory.
 * WordPress will automatically detect and load these style variations.
 */